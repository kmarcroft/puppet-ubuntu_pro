name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: "Build & Publish to Forge"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Run puppet-lint
        run: bundle exec rake lint

      - name: Run puppet syntax check
        run: bundle exec rake syntax

      - name: Run unit tests
        run: bundle exec rake spec

      - name: Build module package
        run: bundle exec build-puppet-module

      - name: Upload release artifact
        uses: actions/upload-artifact@v5
        with:
          name: puppet-module
          path: pkg/*.tar.gz

      # TODO: Uncomment when ready to publish to Puppet Forge
      # - name: Publish to Puppet Forge
      #   env:
      #     FORGE_API_KEY: ${{ secrets.FORGE_API_KEY }}
      #   run: |
      #     bundle exec puppet module push pkg/marckri-ubuntu_pro-*.tar.gz \
      #       --module_group=marckri \
      #       --render-as=json
